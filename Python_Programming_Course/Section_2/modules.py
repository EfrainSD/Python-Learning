# To import a module
import math
# other option is
from math import * # This is the best way because, you can just put the functionName to call it instead of package.functionName

# To import a function from a module
from math import sqrt

# If we want to give a name
from random import randint as randomInteger